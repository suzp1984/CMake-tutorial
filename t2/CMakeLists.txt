PROJECT(HELLO)

ADD_SUBDIRECTORY(src bin)

INSTALL(FILES COPYRIGHT README DESTINATION share/doc/t2)
INSTALL(DIRECTORY doc/ DESTINATION share/doc/t2)
INSTALL(PROGRAMS hello.sh
                 DESTINATION bin
                 PERMISSIONS OWNER_EXECUTE)

